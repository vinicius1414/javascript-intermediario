// Como criar um array

const arr = [1, 2, 3];
const arr2 = new Array(1, 2, 3);

const persons = Array.of("John", "Cris", "Jenny");


const divs = document.querySelectorAll('div');
const arr3 = Array.from(divs);
// Estamos pegando o conteúdo de 'divs' e o transformando em um array, na 'arr3'


/* 

    Todos os modos acima servem para criar um Array,
    Cada um com suas vantagens e desvantagens.

*/








