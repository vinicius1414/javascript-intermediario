// Retorna um novo tipo de dado iterando cada posição de um Array

const arr = [1, 2, 3, 4, 5];

arr.reduce((total, value) => total += value, 0);
// Resultado: 15
// No caso, foi somado todos os elementos da Array, transformando em um inteiro


