// Retorna um novo Array, de mesmo tamanho, iterando cada item de um Array

const arr = [1, 2, 3, 4, 5];

arr.map(value => value * 2);
// Resultado: [2, 4, 6, 8, 10]
// Está multiplicando cada elemento do Array por 2


